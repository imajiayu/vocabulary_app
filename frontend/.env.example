# ===================================
# 前端环境变量配置（VITE_ 前缀，Vite 自动注入到 import.meta.env）
# ===================================
# 复制此文件为 .env.local 并填入实际值
#
# ⚠️ 警告：VITE_ 前缀的变量会被 Vite 构建时硬编码进 JS bundle，对所有访客可见。
#    只能放公开凭证（anon key / 受 RLS 保护的资源）。
#    禁止放任何需要保密的 API key（例如上游 LLM key —— 已通过 ai-proxy Edge Function 代理）。

# --- 必填 ---

# Supabase 项目 URL
VITE_SUPABASE_URL=https://xxx.supabase.co

# Supabase Anonymous Key（公开，RLS 保护数据安全）
VITE_SUPABASE_ANON_KEY=your-anon-key

# --- 可选：后端 API 基址 ---

# 本地开发留空（走 Vite 代理 /api → 后端 :5001）
# 生产填后端完整 URL，如 https://mieltsm.top
VITE_API_BASE_URL=

# --- 可选：Google Cloud 公开密钥（按需） ---

# Google Cloud STT API Key（语音转录；未设置则该功能不可用）
VITE_GOOGLE_STT_API_KEY=

# Google Cloud TTS API Key（非英语单词发音；未设置则该功能不可用）
VITE_GOOGLE_TTS_API_KEY=
